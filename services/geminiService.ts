
// Bu dosya, uygulamanın statik versiyonu için temizlenmiştir.
// GitHub Pages üzerinde API anahtarı güvenliği riski olmaması için AI özellikleri devre dışı bırakıldı.

export const getSmartCommand = async (userQuery: string): Promise<string> => {
  return "";
};

export const explainCommand = async (command: string): Promise<string> => {
  return "AI özellikleri bu sürümde devre dışıdır.";
};
